diff -ruN --ex a/.medusa/server/src/providers/shiprocket/client/index.js b/.medusa/server/src/providers/shiprocket/client/index.js
--- a/.medusa/server/src/providers/shiprocket/client/index.js	2026-09-09 23:52:15
+++ b/.medusa/server/src/providers/shiprocket/client/index.js	2026-09-17 13:58:28
@@ -181,7 +181,7 @@
                 shipping.postal_code === billing.postal_code);
             // Build order payload
             const orderData = {
-                order_id: `${order.id}-${Math.floor(Date.now() / 1000)}`,
+                order_id: `CS-${order.display_id || order.id}-${Math.floor(Date.now() / 1000)}`,
                 order_date: orderDate,
                 pickup_location: this.pickup_location || "Primary",
                 billing_customer_name: (0, validation_1.requireField)(billing.first_name || shipping.first_name, "Billing First Name"),
diff -ruN --ex a/.medusa/server/src/providers/shiprocket/utils/validation.js b/.medusa/server/src/providers/shiprocket/utils/validation.js
--- a/.medusa/server/src/providers/shiprocket/utils/validation.js	2026-09-13 13:25:21
+++ b/.medusa/server/src/providers/shiprocket/utils/validation.js	2026-09-13 13:26:24
@@ -21,14 +21,10 @@
     }
     // Remove all non-numeric characters
     let cleaned = phone.toString().replace(/[^0-9]/g, "");
-    // Strip leading 91 country code if present (+91XXXXXXXXXX → XXXXXXXXXX)
-    if (cleaned.length === 12 && cleaned.startsWith("91")) {
-        cleaned = cleaned.slice(2);
-    }
-    // Strip leading 0 if present (0XXXXXXXXXX → XXXXXXXXXX)
-    if (cleaned.length === 11 && cleaned.startsWith("0")) {
-        cleaned = cleaned.slice(1);
-    }
+    // Strip +91 country code (e.g., +919901915009 -> 9901915009)
+    if (cleaned.length === 12 && cleaned.startsWith("91")) cleaned = cleaned.slice(2);
+    // Strip leading 0 (e.g., 09901915009 -> 9901915009)
+    if (cleaned.length === 11 && cleaned.startsWith("0")) cleaned = cleaned.slice(1);
     // Convert to number
     const num = Number(cleaned);
     // Validate
